<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.gestion.ui.restauration.RestaurationMenusController"
            fitToWidth="true" fitToHeight="true" styleClass="modern-scroll-pane"
            vbarPolicy="AS_NEEDED" hbarPolicy="NEVER"
            stylesheets="@../../styles/card-view.css">
    
    <VBox spacing="20" styleClass="modern-panel" prefWidth="1000">
        <padding>
            <Insets top="24" right="24" bottom="24" left="24"/>
        </padding>

        <HBox spacing="16" alignment="CENTER_LEFT">
            <Label text="📋 Gestion des Menus" styleClass="modern-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="statusInfoLabel" text="Prêt" styleClass="status-info"/>
        </HBox>

        <HBox spacing="12" alignment="CENTER_LEFT" styleClass="action-bar">
            <Button text="➕ Nouveau" onAction="#onNouveauMenu" styleClass="btn-primary"/>
            <Button text="🔄 Actualiser" onAction="#onActualiser" styleClass="btn-secondary"/>
            <Button text="✏️ Modifier" onAction="#onModifier" styleClass="btn-warning" disable="true" fx:id="btnModifier"/>
            <Button text="🗑️ Supprimer" onAction="#onSupprimer" styleClass="btn-danger" disable="true" fx:id="btnSupprimer"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="Filtrer par option:"/>
            <ComboBox fx:id="filterOptionCombo" prefWidth="180" promptText="Toutes" onAction="#onFiltrerOption"/>
        </HBox>

        <VBox spacing="8" VBox.vgrow="ALWAYS">
            <HBox spacing="8" alignment="CENTER_LEFT">
                <Label text="📊 Liste des Menus" styleClass="section-title"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Label fx:id="countLabel" text="0 menus" styleClass="count-label"/>
            </HBox>
            <ListView fx:id="listView" VBox.vgrow="ALWAYS" minHeight="350" prefHeight="400" 
                       styleClass="modern-list-view" onMouseClicked="#onListClick">
                <placeholder>
                    <VBox alignment="CENTER" spacing="12">
                        <Label text="📭 Aucune donnée disponible" styleClass="placeholder-text"/>
                    </VBox>
                </placeholder>
            </ListView>
        </VBox>

        <VBox spacing="12" styleClass="form-section">
            <Label text="✏️ Formulaire" styleClass="section-title"/>
            <GridPane hgap="12" vgap="12">
                <columnConstraints>
                    <ColumnConstraints minWidth="100" prefWidth="120"/>
                    <ColumnConstraints minWidth="200" prefWidth="300" hgrow="ALWAYS"/>
                    <ColumnConstraints minWidth="100" prefWidth="120"/>
                    <ColumnConstraints minWidth="150" prefWidth="200" hgrow="ALWAYS"/>
                </columnConstraints>
                
                <Label text="Nom *:" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
                <TextField fx:id="inputNom" promptText="Nom du menu" 
                           styleClass="input-field" GridPane.columnIndex="1" GridPane.rowIndex="0"/>
                
                <Label text="Option ID:" GridPane.columnIndex="2" GridPane.rowIndex="0"/>
                <TextField fx:id="inputOptionId" promptText="ID option" 
                           styleClass="input-field" GridPane.columnIndex="3" GridPane.rowIndex="0"/>
                
                <Label text="Actif:" GridPane.columnIndex="0" GridPane.rowIndex="1"/>
                <CheckBox fx:id="inputActif" text="Menu actif" selected="true" GridPane.columnIndex="1" GridPane.rowIndex="1"/>
            </GridPane>
            
            <Label fx:id="validationMessage" text="" styleClass="validation-message" visible="false" managed="false"/>
            
            <HBox spacing="12" alignment="CENTER_LEFT">
                <Button text="💾 Enregistrer" onAction="#onEnregistrer" styleClass="btn-success" fx:id="btnEnregistrer"/>
                <Button text="✏️ Modifier" onAction="#onModifier" styleClass="btn-warning" fx:id="btnModifierForm" disable="true"/>
                <Button text="❌ Annuler" onAction="#onAnnuler" styleClass="btn-secondary"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="🔄 Réinitialiser" onAction="#onNouveauMenu" styleClass="btn-secondary"/>
            </HBox>
        </VBox>
    </VBox>
</ScrollPane>
