<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<!-- Page liste uniquement : pas de formulaire sur cette page. Le formulaire s'ouvre dans une fenêtre séparée. -->
<VBox xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.gestion.ui.restauration.RestaurationOptionsListeController"
      spacing="20" styleClass="modern-panel" prefWidth="1000" VBox.vgrow="ALWAYS"
      stylesheets="@../../styles/card-view.css">
    <padding>
        <Insets top="24" right="24" bottom="24" left="24"/>
    </padding>

    <HBox spacing="16" alignment="CENTER_LEFT">
        <Label text="📊 Liste des Options de Restauration" styleClass="modern-title"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label fx:id="statusInfoLabel" text="Prêt" styleClass="status-info"/>
    </HBox>

    <HBox spacing="12" alignment="CENTER_LEFT" styleClass="action-bar">
        <Button text="➕ Nouvelle" onAction="#onNouvelleOption" styleClass="btn-primary"/>
        <Button text="🔄 Actualiser" onAction="#onActualiser" styleClass="btn-secondary"/>
        <Button text="✏️ Modifier" onAction="#onModifier" styleClass="btn-warning" disable="true" fx:id="btnModifier"/>
        <Button text="🗑️ Supprimer" onAction="#onSupprimer" styleClass="btn-danger" disable="true" fx:id="btnSupprimer"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label text="Type événement:"/>
        <TextField fx:id="filterTypeField" promptText="SOIREE, RANDONNEE..." prefWidth="150" onKeyReleased="#onFiltrer"/>
        <Button text="🔍 Filtrer" onAction="#onFiltrer" styleClass="btn-secondary"/>
    </HBox>

    <VBox spacing="8" VBox.vgrow="ALWAYS">
        <HBox spacing="8" alignment="CENTER_LEFT">
            <Label text="Options" styleClass="section-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="countLabel" text="0 options" styleClass="count-label"/>
        </HBox>
        <ListView fx:id="listView" VBox.vgrow="ALWAYS" minHeight="350" prefHeight="400"
                   styleClass="modern-list-view" onMouseClicked="#onListClick">
            <placeholder>
                <VBox alignment="CENTER" spacing="12">
                    <Label text="📭 Aucune donnée disponible" styleClass="placeholder-text"/>
                </VBox>
            </placeholder>
        </ListView>
    </VBox>
</VBox>
